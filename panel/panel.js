// Currently not used. Still we keep it as a base to build up.
chrome.devtools.panels.create("Server",
    "icon.png",
    "panel/panel.html",
    function(panel) {
        // code invoked on panel creation
        // console.log("Panel created");
    }
);